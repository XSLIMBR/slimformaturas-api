 version: 2

 jobs:
   build:
     steps:
     - checkout
     - run:
         name: Restore
         command: dotnet restore
         working_directory: SlimFormaturas.Api
     - run:
         name: Build
         command: dotnet build -c Release
         working_directory: SlimFormaturas.Api
